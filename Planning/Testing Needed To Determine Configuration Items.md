Accelerometer

* Digital low pass filter \- what is the optimal bandwidth and delay tradeoff. Lower bandwidth means less noise (and less false positives), but a greater latency  
* Determine correct full scale range. \+-2g is probably sufficient, but need to make sure. 